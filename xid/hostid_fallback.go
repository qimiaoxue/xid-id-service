//go:build (!darwin && ignore) || (!linux && ignore) || (!freebsd && !windows)
// +build !darwin,ignore !linux,ignore !freebsd,!windows

package xid

/*
import "errors"

func readPlatformMachineID() (string, error) {
	return "", errors.New("not implemented")
}
*/
